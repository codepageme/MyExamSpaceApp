<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Teacher - MyExamSpace.Com{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('Css/bootstrap.css') }}">
     <script src="{{ asset('js/jquery-3.7.1.min.js') }}"></script>
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
    {% block stylesheets %}
       
    {% endblock %}
</head>
<body>
    <div id="map">
        {% block map_content %} 
        
        {% endblock %}
        </div>
    

    <div id="screen">

              <div id="loader">
        {% include 'partial/pload.html.twig' %}
    </div>  
    
        {% block content %}
        <!-- Default content, can be overridden in child templates -->
        {% endblock %}
        <br>
        <hr>
            <!-- Footer container -->
            <div id="footer-container" class="footer-container">
                <!-- Toggle button -->
                <button id="footer-toggle" class="footer-toggle btn btn-primary">
                    <span class="glyphicon glyphicon-question-sign"> Click-Me </span>
                </button>
                <!-- Footer content -->
                <div id="footer-content" class="footer-content">
                    <!-- Your footer content here -->
                       <footer>
                        <p class="footsign">- Copyright &copy; <a href="https://codepagehere.github.io/happycode/">HappyCode</a> / <a href="https://codepagehere.github.io/graphitie/">Graphitie</a>; MyExamspaceApp. 2024 - {{ "now"|date("Y") }} | All Rights Reserved -</p>
                        <p><i> MyExamSpaceApp is licensed to @newuser for [specific use cases], with HappyCode / Graphitie retaining the right to license the product to third parties. </i></p>
                    </footer>
                </div>
            </div>
    </div>

    <script>
 // Get footer toggle button and content elements
var footerToggle = $('#footer-toggle');
var footerContent = $('#footer-content');

// Toggle footer content on button click
footerToggle.on('click', function () {
    if (footerContent.hasClass('footer-content-scroll-in')) {
        footerContent.removeClass('footer-content-scroll-in');
        footerContent.addClass('footer-content-scroll-out');
        setTimeout(function () {
            footerContent.slideUp();
        }, 500);
    } else {
        footerContent.slideDown();
        footerContent.removeClass('footer-content-scroll-out');
        footerContent.addClass('footer-content-scroll-in');
    }
});

 // Ensure the loader is hidden once the page is fully loaded
            window.addEventListener('load', function () {
                // Wait for the page to load, then hide the loader
                var loader = document.getElementById('loader');
                if (loader) {
                    loader.style.display = 'none';
                }
            });

            // In case the page is already loaded when this script is run (for cache scenarios)
            if (document.readyState === 'complete') {
                document.getElementById('loader').style.display = 'none';
            }
            
</script>

<style>

/* Footer container styles */
.footer-container {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 85%;
    z-index: 1000; /* Ensure footer is above all other elements */
}

/* Toggle button styles */
.footer-toggle {
    position: absolute;
    bottom: 25px;
    left: 240px;
    border: none;
    cursor: pointer;
}

/* Footer content styles */
.footer-content {
    display: none;
    background-color: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    width:100%;
    margin-left:250px;
}


/* Full-screen loader */
        #loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: white; /* White background to cover all content */
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999; /* Ensures loader stays above all other elements */
        }


</style>

   
</body>
</html>
